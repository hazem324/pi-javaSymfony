<div class="container-fluid topbar bg-light px-5 d-none d-lg-block">
    <div class="row gx-0 align-items-center">
        <div class="col-lg-8 text-center text-lg-start mb-2 mb-lg-0">
            <div class="d-flex flex-wrap">
                <a href="tel:+01234567890" class="text-muted small me-4">
                    <i class="fas fa-phone-alt text-primary me-2"></i>+216 88 888 888</a>
                <a href="mailto:example@gmail.com" class="text-muted small me-0">
                    <i class="fas fa-envelope text-primary me-2"></i>culturespaceTeam@gmail.com</a>
            </div>
        </div>
        <div class="col-lg-4 text-center text-lg-end">
            <div class="d-inline-flex align-items-center" style="height: 45px;">

                {% if not app.user %}
                    <a href="{{ path('app_home_signup') }}">
                        <small class="me-3 text-dark">
                            <i class="fa fa-user text-primary me-2"></i>Register
                        </small>
                    </a>


                    <a href="{{ path('app_home_signin') }}">

                        <small class="me-3 text-dark">
                            <i class="fa fa-sign-in-alt text-primary me-2"></i>Login
                        </small>
                    </a>

                {% else %}
                    <!-- Display user's details if logged in -->
                    <div class="d-flex align-items-center me-3">
                        <img class="rounded-circle me-lg-2" src="{{ app.user.profileIMG ? asset(app.user.profileIMG) : asset('img/team-3.jpg') }}" alt="" style="width: 40px; height: 40px;">
                        <span class="ms-2 text-dark">
                            {{ app.user.firstName }} {{ app.user.lastName }}
                        </span>
                    </div>

                {% endif %}

                <div class="dropdown">
                    <a href="#" class="dropdown-toggle text-dark" data-bs-toggle="dropdown">
                        <small>
                            <i class="fa fa-home text-primary me-2"></i>
                            My Dashboard
                        </small>
                    </a>
                    <div class="dropdown-menu rounded">


                        {% if app.user %}
                            <a href="{{ path('app_edit_profile_user', {'id': app.user.id}) }}" class="dropdown-item">
                                <i class="fas fa-cog me-2"></i> My Profile
                            </a>
                            <a href="{{ path('app_logout') }}" class="dropdown-item">
                                <i class="fas fa-power-off me-2"></i> Log Out
                            </a>

                        {% endif %}
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>
