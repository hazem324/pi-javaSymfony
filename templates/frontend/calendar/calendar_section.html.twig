{# templates/frontend/calendar_section.html.twig #}

<!-- Calendar Section Start -->
<div class="container-fluid calendar-section py-5">
    <div class="container">
        <h2 class="text-center mb-5">Calendrier des Événements Culturels</h2>
        <p class="text-center mb-5">Découvrez les événements culturels en Tunisie, Algérie, Maroc et Mauritanie.</p>
        <div id="calendar" class="wow fadeInUp" data-wow-delay="0.2s"></div>
    </div>
</div>
<!-- Calendar Section End -->

<style>
    .calendar-section {
        background-color: #f8f9fa;
    }
    #calendar {
        max-width: 900px;
        margin: 0 auto;
    }
    .fc-event {
        background-color: #A7F3D0 !important; /* Nouvelle couleur */
        border: none;
        padding: 5px;
        border-radius: 5px;
        color: #000; /* Texte noir pour une meilleure lisibilité */
        font-weight: bold;
        text-align: center;
        white-space: normal !important; /* Allow text to wrap to the next line */
        word-wrap: break-word; /* Break long words if necessary */
        overflow: hidden; /* Hide overflow */
        display: flex; /* Use flexbox for better alignment */
        align-items: center; /* Center text vertically */
        justify-content: center; /* Center text horizontally */
        height: 100%; /* Ensure the event takes up the full height */
    }
    .fc-event:hover {
        background-color: #32CD80 !important; /* Assombrir légèrement au survol */
    }
    .fc-event .fc-event-title {
        white-space: normal !important; /* Ensure the event title wraps */
        word-wrap: break-word; /* Break long words if necessary */
    }
    .fc-toolbar-title {
        font-size: 1.5rem;
        font-weight: bold;
        color: #333;
    }
    .fc-button {
        background-color:rgb(0, 179, 164) !important;
        border: none;
        color: #000;
    }
    .fc-button:hover {
         background-color: rgb(0, 200, 180) !important;
    }
</style>