<div class="container-fluid position-relative p-0">
    <nav class="navbar navbar-expand-lg navbar-dark bg-dark px-4 px-lg-5 py-3 py-lg-0">
        <a href="" class="navbar-brand p-0">
            <h1 class="text-primary m-0">Culture Space</h1>
            <!-- Removed extra commented-out content for clarity -->
        </a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarCollapse">
            <span class="fa fa-bars"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarCollapse">
            <div class="navbar-nav ms-auto py-0">
                <a href="{{ path('app_home') }}" class="nav-item nav-link text-light {% if app.request.get('_route') == 'app_home' %}active{% endif %}">Home</a>
                <a href="{{ path('app_events') }}" class="nav-item nav-link text-light {% if app.request.get('_route') starts with 'app_event' %}active{% endif %}">Events</a>
                <a href="{{ path('product_list') }}" class="nav-item nav-link text-light {% if app.request.get('_route') starts with 'product_' %}active{% endif %}">Market-Place</a>
                <a href="{{ path('app_groupList') }}" class="nav-item nav-link text-light {% if app.request.get('_route') starts with 'app_group' %}active{% endif %}">Community</a>
                <a href="{{ path('app_contact') }}" class="nav-item nav-link text-light {% if app.request.get('_route') == 'app_contact' %}active{% endif %}">Contact Us</a>
                {# Uncomment and fix dropdown if needed
                <div class="nav-item dropdown">
                    <a href="#" class="nav-link text-light dropdown-toggle" data-bs-toggle="dropdown">Pages</a>
                    <div class="dropdown-menu m-0">
                        <a href="{{ path('app_feature') }}" class="dropdown-item">Our Features</a>
                        <a href="{{ path('app_team') }}" class="dropdown-item">Our Team</a>
                        <a href="{{ path('app_testimonial') }}" class="dropdown-item">Testimonial</a>
                        <a href="{{ path('app_offer') }}" class="dropdown-item">Our Offer</a>
                        <a href="{{ path('app_faq') }}" class="dropdown-item">FAQs</a>
                    </div>
                </div>
                #}
                <a href="#" class="btn btn-primary rounded-pill py-2 px-4 my-3 my-lg-0 flex-shrink-0">Get Started</a>

            </div>
        </div>
    </nav>

    {# {% if app.request.get('_route') != 'app_events' and app.request.get('_route') != 'app_event_details' and app.request.get('_route') != 'app_event_register' %}
        {% include 'frontend/home/carousel.html.twig' %}
    {% endif %} #}
</div>