
{% block title %}Commande Validée{% endblock %}

{% block body %}
<div class="container my-5">
    <div class="alert alert-success text-center" role="alert">
        <h4 class="alert-heading">Commande Validée !</h4>
        <p>{{ message }}</p>
        <hr>
        <p class="mb-0">Vous pouvez consulter vos commandes ou revenir à la <a href="{{ path('product_list') }}">liste des produits</a>.</p>
    </div>

    <div class="d-flex justify-content-center mt-4">
        <a href="{{ path('product_list') }}" class="btn btn-outline-primary me-3">Retour aux produits</a>
        <a href="{{ path('panier_index') }}" class="btn btn-outline-secondary">Revenir au panier</a>
    </div>
</div>
{% endblock %}
    