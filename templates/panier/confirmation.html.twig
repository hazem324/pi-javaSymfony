{% extends 'base.html.twig' %}

{% block title %}Commande Validée !{% endblock %}

{% block body %}



<div class="container text-center">
    <div class="alert alert-success mt-4">
        <h2>Commande Validée !</h2>
        <p>Votre commande a été validée avec succès !</p>
    </div>
    <div class="text-center mt-4">
        <a href="{{ path('product_list') }}" class="btn btn-success">Retour aux produits</a>
        <a href="{{ path('app_home') }}" class="btn btn-dark">Retour à l'accueil</a>
    </div>
</div>


{% endblock %}
